# Application Environment
# Options: local, qa, prod
APP_ENV=local
# -> Application URL:

# Example: http://localhost:3000 or https://yourdomain.com
APP_URL=http://localhost:3000

# -> JWT Configuration:

# Secret key for JWT token signing (REQUIRED)
# Generate a strong random string for production
WEBTOKEN_SECRET_KEY=your-super-secret-jwt-key-change-this-in-production

# JWT Token expiration time in seconds (default: 86400 = 24 hours)
WEBTOKEN_EXPIRATION_TIME=86400
# WalletConnect (optional)
# WALLET_CONNECT_PROJECT_ID=
# Database Configuration
# MongoDB connection URL (REQUIRED)
# Format: mongodb://username:password@host:port/database
# For local development with Docker: mongodb://localhost:27017/nest
DB_URL=mongodb://localhost:27017/nest

# -> S3 Bucket Configuration:

MINIO_ENDPOINT=url-here
MINIO_ACCESS_KEY=access-key
MINIO_SECRET_KEY=secret-key
MINIO_BUCKET_NAME=bucket-name


# -> Solana Configuration:

# Solana RPC endpoint URL (optional, defaults to mainnet-beta)
# For mainnet: https://api.mainnet-beta.solana.com
# For devnet: https://api.devnet.solana.com
# For testnet: https://api.testnet.solana.com
# Custom RPC: https://your-custom-rpc-endpoint.com
SOLANA_RPC_URL=https://api.mainnet-beta.solana.com

JUPITER_API_BASE_URL=https://lite-api.jup.ag
SOLANA_VAULT_FACTORY_ADDRESS=FactoryPublicKeyHere

SOLANA_ADMIN_PRIVATE_KEY=admin_wallet_private_key

# Optional: MongoDB Authentication (if using authenticated MongoDB)
# MONGO_USERNAME=your_mongo_username
# MONGO_PASSWORD=your_mongo_password

# Optional: Application Port (default: 3000)
# PORT=3000

# Optional: Logging Level
# LOG_LEVEL=info

# Optional: Rate Limiting
# RATE_LIMIT_MAX=100
# RATE_LIMIT_WINDOW_MS=60000

# -> CORS Configuration (Environment-Specific)

# Environment-specific CORS origins (comma-separated)
# Add as many origins as needed - no limit!

# Local Development Origins
LOCAL_ORIGINS=http://localhost:3000,http://localhost:3001,http://127.0.0.1:3000,http://127.0.0.1:3001

# QA Environment Origins
QA_ORIGINS=https://qa.defimarkets.blocsys.com,https://admin-qa.defimarkets.blocsys.com,https://api-qa.defimarkets.blocsys.com

# Production Environment Origins
PROD_ORIGINS=https://defimarkets.blocsys.com,https://admin.defimarkets.blocsys.com,https://api.defimarkets.blocsys.com

# Optional: Swagger Configuration
# SWAGGER_TITLE=API
# SWAGGER_DESCRIPTION=API Description
# SWAGGER_VERSION=1.0
# SWAGGER_PATH=api/docs

# Helius webhook secret (REQUIRED for webhook verification)
HELIUS_WEBHOOK_SECRET=replace-with-strong-random-secret

# Helius API key (optional if you call Helius APIs)
# HELIUS_API_KEY=your-helius-api-key

# Helius Stream (optional)
# Your Helius API key for outbound requests to Helius
# HELIUS_API_KEY=your-helius-api-key
# Secret used to verify incoming Helius webhook requests
# HELIUS_WEBHOOK_SECRET=replace-with-strong-random-secret

# NEW RELIC LICENSE key
NEW_RELIC_LICENSE_KEY=your_key_here

# -> Redis Configuration:

# Redis host (default: localhost)
REDIS_HOST=localhost
# Redis port (default: 6379)
REDIS_PORT=6379
# Redis password (optional)
# REDIS_PASSWORD=your_redis_password
# Redis database number (default: 0)
REDIS_DB=0
# Redis cache TTL in seconds (default: 300 = 5 minutes)
REDIS_TTL=300
# Redis max items in cache (default: 100)
REDIS_MAX_ITEMS=100

# -> External APIs:


# Jupiter Swap APIs
JUPITER_QUOTE_API=https://lite-api.jup.ag/swap/v1/quote
JUPITER_SWAP_API=https://lite-api.jup.ag/swap/v1/swap-instructions

#Raydium token List API
RAYDIUM_API_BASE_URL=

#ORCA token list api
ORCA_API_BASE_URL=


# -> Rate Limit config

# eg. 10 request count in 1 seconds
RATE_LIMIT_REQ_COUNT=
# eg. Here 1000ms  means 1 second
RATE_LIMIT_TIME_WINDOW=

# -> CRON Job Interval
CRON_JOB_INTERVAL=

# -> Cool Down time Interval
COOLDOWN_PERIOD=


SOLANA_NETWORK=mainnet-beta


# -> Twitter OAuth Configuration:

# Backend base URL (used for OAuth callback URL)
BASE_URL=http://localhost:3400

# Twitter OAuth 2.0 Client ID (from Twitter Developer Portal)
TWITTER_CLIENT_ID=your_twitter_client_id

# Twitter OAuth 2.0 Client Secret (from Twitter Developer Portal)
TWITTER_CLIENT_SECRET=your_twitter_client_secret

# Frontend URL to redirect users after successful authentication
CLIENT_HOME_PAGE_URL=http://localhost:3000

# OAuth client type: 'confidential' for server-side apps, 'public' for client-side apps
CLIENT_TYPE=confidential

# Session secret for session management and security
# Used for session integrity, CSRF protection, and secure session operations
# Generate a strong random string: openssl rand -hex 32
SESSION_SECRET=your-session-secret-change-this

# Optional: Custom redirect URI if different from BASE_URL/api/v1/auth/twitter/callback
# REDIRECT_URI=http://localhost:3400/api/v1/auth/twitter/callback